      - name: Debug GCP Authentication
        run: |
          echo "Checking authenticated accounts..."
          gcloud auth list
          echo "Checking access to the bucket..."
          gsutil ls gs://jrt2301-actions/
